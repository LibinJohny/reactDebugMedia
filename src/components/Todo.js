import PropTypes from 'prop-types'
import React, { Component } from 'react'

export default class Todo extends Component {
  state = {
    todoState : 'Todo component'
  }

  render() {
    return (
      <div>Todo component</div>
    )
  }
}
